node_modules
.next
out
build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env*.local
.vercel
coverage
prisma/migrations/dev.db
design.tar.gz
design_source/
# NOTA: `.docs` NO se ignora — es la documentación canónica del proyecto y está
# versionada (reemplazó al árbol `docs/`, que era un duplicado desactualizado).
# Estuvo ignorado por error desde el "pre-production commit"; con la regla activa,
# cualquier documento NUEVO en .docs/ quedaba fuera de git sin aviso.